# Формат Excel для сборки IDW-альбома

## Лист и заголовки
- Имя листа: `ALBUM`.
- Первая строка — заголовки столбцов (регистр не важен).
- Начиная со 2-й строки — данные по моделям.

## Столбцы
### Обязательный
- `MODEL_PATH` — путь к `.ipt`.
  - Можно абсолютный путь.
  - Можно относительный путь (поиск идёт сначала от Workspace активного проекта Inventor, затем от папки Excel-файла).

### Необязательные (prompt-поля штампа)
- `CODE`
- `PROJECT_NAME`
- `DRAWING_NAME`
- `ORG_NAME`
- `STAGE`
- `SHEET`
- `SHEETS`

Если поле пустое:
- для `SHEET` и `SHEETS` используется автозаполнение (номер листа и общее число листов);
- для остальных полей используются значения по умолчанию из макроса.

## Соответствие колонок Excel и prompted-полей штампа
- `CODE` -> `<Prompt>CODE</Prompt>`
- `PROJECT_NAME` -> `<Prompt>PROJECT_NAME</Prompt>`
- `DRAWING_NAME` -> `<Prompt>DRAWING_NAME</Prompt>`
- `ORG_NAME` -> `<Prompt>ORG_NAME</Prompt>`
- `STAGE` -> `<Prompt>STAGE</Prompt>`
- `SHEET` -> `<Prompt>SHEET</Prompt>`
- `SHEETS` -> `<Prompt>SHEETS</Prompt>`

## Пример таблицы
| MODEL_PATH | CODE | PROJECT_NAME | DRAWING_NAME | ORG_NAME | STAGE | SHEET | SHEETS |
|---|---|---|---|---|---|---|---|
| `001.ipt` | `000-2026-АР` | `Многоквартирный жилой дом` | `План на отм. 0.000` | `ООО "Ромашка"` | `П` | `1` | `3` |
| `sub\002.ipt` | `000-2026-АР` | `Многоквартирный жилой дом` | `План на отм. +3.000` | `ООО "Ромашка"` | `П` | `2` | `3` |
| `D:\Models\003.ipt` | `000-2026-АР` | `Многоквартирный жилой дом` | `План кровли` | `ООО "Ромашка"` | `П` | `3` | `3` |
