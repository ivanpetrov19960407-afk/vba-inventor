# VBA Inventor: RKM рамка + штамп (первая рабочая версия)

## Что делает проект
Этот репозиторий содержит **только исходники VBA** для Autodesk Inventor, которые создают и применяют:
- отдельный `BorderDefinition` (внутренняя рамка A3 landscape с отступами 20/5 мм);
- отдельный `TitleBlockDefinition` (штамп формы 1, 185x55 мм);
- идемпотентное повторное применение на лист.

## Текущий scope
В этой фазе реализованы только:
1. рамка;
2. штамп;
3. публичная точка входа запуска.

## Что намеренно **вне scope** сейчас
- верхние таблицы «Ведомость...»;
- нижний левый технический текстовый блок;
- экспорт в `.idw/.dwg`, сборки, ZIP, скриншоты.

## Структура
- `src/RKM_EntryPoints.bas` — публичные макросы запуска.
- `src/RKM_FrameBorder.bas` — создание/применение BorderDefinition.
- `src/RKM_TitleBlockPrompted.bas` — создание/применение TitleBlockDefinition.
- `src/RKM_Utils.bas` — общие функции, единицы/константы, безопасная очистка и поиск ресурсов.
- `docs/*.md` — спецификация, ручной чек-лист, ограничения.

## Импорт модулей в Inventor VBA
1. Откройте Inventor.
2. Откройте VBA Editor (обычно `Alt+F11`).
3. `File -> Import File...` и импортируйте все `.bas` из `src/`.
4. Выполните `Debug -> Compile VBAProject`.

### Важно про `Attribute VB_Name`
- При **Import File** строку `Attribute VB_Name = "..."` оставляйте как есть.
- При **ручной вставке кода** в окно модуля сначала удалите строку `Attribute VB_Name`, затем вставляйте остальной код.

## Как запускать макрос
Рекомендуемый запуск:
- откройте чертёж Inventor (`.idw/.dwg`) и активируйте нужный лист;
- запустите `Rkm_CreateOrApplyA3Frame`.

Что делает макрос:
- проверяет, что активный документ — `DrawingDocument`;
- переводит активный лист в формат `A3 Landscape`;
- создаёт или обновляет ресурсы `RKM_A3_BORDER` и `RKM_A3_TITLEBLOCK`;
- удаляет текущие `Sheet.Border` и `Sheet.TitleBlock`;
- заново применяет рамку и штамп без дубликатов.

## Стратегия по повторному запуску
- Фиксированные имена ресурсов: `RKM_A3_BORDER`, `RKM_A3_TITLEBLOCK`.
- При запуске макрос редактирует существующие definitions (или создаёт их, если отсутствуют).
- Перед вставкой на лист текущие `Sheet.Border` и `Sheet.TitleBlock` удаляются безопасно.
- Повторный запуск не создает дубликаты и сохраняет ту же геометрию.

## Кодировка и языковые caveats
- В `.bas` не используются русские строковые литералы: русские подписи собираются через `ChrW(...)` (функция `RuText`).
- Это снижает риск «кракозябр» при импорте модулей в Inventor VBA из разных кодировок.

## Проверка результата
См. пошаговый чек-лист: `docs/manual-test-checklist.md`.
